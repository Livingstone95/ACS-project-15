---
plugin: amazon.aws.aws_ec2

boto_profile: aws_profile

regions:
  - eu-central-1
  
keyed_groups:
  # add hosts to tag_Name_value groups for each aws_ec2 host's tags.Name variable
  - key: tags.Name
    prefix: tag_nginx_david_prj_15
    separator: ""
